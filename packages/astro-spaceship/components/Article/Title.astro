---
import type { Document } from "../../types";

export type Props = {
  document: Document;
};

const { title } = Astro.props.document.data;
---

<h1 data-pagefind-weight="10" class="article-title">{title}</h1>
